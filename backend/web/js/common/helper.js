if (typeof doctorBackend.common == 'undefined') {
    doctorBackend.common = {};
}

doctorBackend.common.helper = (function() {
    return {
        isActive: true,
        init: function () {
            
        }
    };
}());